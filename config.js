// config.js — AnimeStuff Update Panel configuration
// Edit these defaults to match your GitHub setup

const CONFIG = {
  // GitHub repo in "owner/repo" format
  repo: "Karma845/Karma845.github.io",

  // Branch to commit to
  branch: "main",

  // Path to novels.json inside the repo
  novelsJsonPath: "novels.json",

  // Path to updates.json inside the repo
  updatesJsonPath: "updates.json",

  // Base URL for novel pages (used to build the url field)
  baseUrl: "https://animestuff.me/docs/assets/html/",

  // Maximum update entries to keep in updates.json
  maxUpdates: 50,
};
